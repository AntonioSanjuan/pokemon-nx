import { ACCOUNT_FEATURE_KEY } from "../../account.reducer";
import { initialAccountState } from "../../models/accountState.initialState";

export const accountAppStateMock = {
    [ACCOUNT_FEATURE_KEY]: initialAccountState
}